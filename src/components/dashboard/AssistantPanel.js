"use client";
import {useState} from 'react'; import {AnimatePresence,motion} from 'framer-motion'; import {Sparkles,X,Send} from 'lucide-react';
const suggestions=['How many products do we have?','How many orders today?','How many customers?','Show low stock products.','Show latest reviews.'];
export default function AssistantPanel({data}){const [open,setOpen]=useState(false),[answer,setAnswer]=useState('');const ask=q=>{const text=(q||'').toLowerCase();let a=`There are ${data.stats.products} products.`;if(text.includes('order'))a=`There are ${data.stats.orders} orders in the workspace.`;if(text.includes('customer'))a=`There are ${data.stats.customers} customers.`;if(text.includes('low stock'))a=`${data.products.filter(p=>p.status==='Low stock').map(p=>p.name).join(', ')||'No products'} are low in stock.`;if(text.includes('review'))a=`Latest review: “${data.reviews[0]?.body||'No reviews yet.'}”`;setAnswer(a)};return <><button onClick={()=>setOpen(true)} className="inline-flex items-center justify-center gap-2 rounded-xl bg-[var(--primary)] px-4 py-2.5 text-sm font-semibold text-white shadow-lg shadow-indigo-500/20 transition hover:-translate-y-0.5"><Sparkles size={16}/>Ask Nexa AI</button><AnimatePresence>{open&&<motion.div initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}} className="fixed inset-0 z-[60] grid place-items-end bg-slate-950/35 p-4 sm:place-items-center" onClick={()=>setOpen(false)}><motion.section initial={{opacity:0,y:18}} animate={{opacity:1,y:0}} exit={{opacity:0,y:18}} onClick={e=>e.stopPropagation()} className="w-full max-w-lg rounded-3xl border border-[var(--line)] bg-[var(--surface)] p-5 shadow-2xl sm:p-6"><header className="flex items-center justify-between"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[var(--soft)] text-[var(--primary)]"><Sparkles size={19}/></span><div><h2 className="font-semibold">Nexa AI</h2><p className="text-xs muted">Read-only workspace assistant</p></div></div><button onClick={()=>setOpen(false)} className="icon-btn"><X size={18}/></button></header><div className="my-5 rounded-2xl bg-[var(--bg)] p-4 text-sm leading-6">Ask a quick question about your live workspace.</div><div className="flex flex-wrap gap-2">{suggestions.map(s=><button onClick={()=>ask(s)} key={s} className="rounded-lg border border-[var(--line)] px-3 py-2 text-left text-xs transition hover:border-[var(--primary)]">{s}</button>)}</div>{answer&&<div className="mt-5 rounded-2xl bg-[var(--soft)] p-4 text-sm">{answer}</div>}<form onSubmit={e=>{e.preventDefault();ask(new FormData(e.currentTarget).get('question'));e.currentTarget.reset()}} className="mt-5 flex gap-2"><input name="question" className="min-w-0 flex-1 rounded-xl border border-[var(--line)] bg-[var(--surface)] px-3 text-sm outline-none focus:border-[var(--primary)]" placeholder="Ask a question..."/><button className="icon-btn bg-[var(--primary)] text-white"><Send size={16}/></button></form></motion.section></motion.div>}</AnimatePresence></>}

import { pgTable, serial, varchar, text, integer, numeric, timestamp, boolean } from 'drizzle-orm/pg-core';
const timestamps={createdAt:timestamp('created_at').defaultNow().notNull(),updatedAt:timestamp('updated_at').defaultNow().notNull()};
export const admins=pgTable('admins',{id:serial('id').primaryKey(),name:varchar('name',{length:100}).notNull(),email:varchar('email',{length:160}).notNull().unique(),...timestamps});
export const categories=pgTable('categories',{id:serial('id').primaryKey(),name:varchar('name',{length:100}).notNull().unique(),description:text('description'),color:varchar('color',{length:30}).default('indigo'),...timestamps});
export const customers=pgTable('customers',{id:serial('id').primaryKey(),name:varchar('name',{length:120}).notNull(),email:varchar('email',{length:160}).notNull().unique(),location:varchar('location',{length:100}),status:varchar('status',{length:30}).default('Active'),lifetimeValue:numeric('lifetime_value',{precision:12,scale:2}).default('0'),...timestamps});
export const products=pgTable('products',{id:serial('id').primaryKey(),name:varchar('name',{length:140}).notNull(),categoryId:integer('category_id').references(()=>categories.id),price:numeric('price',{precision:10,scale:2}).notNull(),stock:integer('stock').default(0),sold:integer('sold').default(0),status:varchar('status',{length:30}).default('In stock'),...timestamps});
export const orders=pgTable('orders',{id:serial('id').primaryKey(),orderNumber:varchar('order_number',{length:40}).notNull().unique(),customerId:integer('customer_id').references(()=>customers.id),productId:integer('product_id').references(()=>products.id),total:numeric('total',{precision:12,scale:2}).notNull(),status:varchar('status',{length:30}).default('Pending'),paymentStatus:varchar('payment_status',{length:30}).default('Paid'),shippingStatus:varchar('shipping_status',{length:30}).default('Pending'),...timestamps});
export const reviews=pgTable('reviews',{id:serial('id').primaryKey(),customerId:integer('customer_id').references(()=>customers.id),productId:integer('product_id').references(()=>products.id),rating:integer('rating').notNull(),body:text('body').notNull(),published:boolean('published').default(true),...timestamps});

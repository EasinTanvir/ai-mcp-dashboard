import 'server-only'; import {db} from '@/db'; import {products,categories,customers,orders,reviews} from '@/db/schema'; import {count,eq,desc} from 'drizzle-orm';
const total=async table=>(await db.select({value:count()}).from(table))[0]?.value??0;
export const mcpTools={count_products:async()=>`There are ${await total(products)} products.`,count_categories:async()=>`There are ${await total(categories)} categories.`,count_orders:async()=>`There are ${await total(orders)} orders.`,count_customers:async()=>`There are ${await total(customers)} customers.`,count_reviews:async()=>`There are ${await total(reviews)} reviews.`,low_stock_products:async()=>{const rows=await db.select({name:products.name,stock:products.stock}).from(products).where(eq(products.status,'Low stock'));return rows.length?rows.map(x=>`${x.name} (${x.stock})`).join(', '):'No low-stock products.';},recent_orders:async()=>{const rows=await db.select({number:orders.orderNumber,status:orders.status}).from(orders).orderBy(desc(orders.createdAt)).limit(3);return rows.map(x=>`${x.number}: ${x.status}`).join(', ')||'No orders.';}};
export async function runMcpTool(name){const tool=mcpTools[name];if(!tool)throw new Error('Unknown tool');return tool();}
